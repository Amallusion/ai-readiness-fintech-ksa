# Step 7 — Sink

The final destination: a single, self-contained HTML dashboard.

## Files
- `build_dashboard.py` — generates it
- `dashboard.html` — the actual deliverable, just open it in a browser
