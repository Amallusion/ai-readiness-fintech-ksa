# Step 3 — Pre-processor (PP)

Validates Step 2's output and rebases each sector to an index (Jan 2019 = 100)
so sectors of different sizes are comparable. Confirms no missing/zero values.

Key file: sector_matrix_indexed.csv (rebased index, used for Step 4 clustering).
